#include <stdio.h>


int main(void)
{
    int    *p1;
    char   *p2; 
    printf("the sizeof p1 is %u\n",sizeof p1); 
    printf("the sizeof p2 is %u\n",sizeof p2);
    return 0;
}
//the sizeof p1 is 8
//the sizeof p2 is 8